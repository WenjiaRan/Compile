


int add(int x, int y){
    return x+x+y;
}
int main(){

     int y=1;

     return 0;

 }
 // TODO: 数组定义已经完成, 数组调用和赋值已经完成, 只剩下面的文法了!!! 卧槽, 感觉好难, 明天看看
 //FuncFParam   → BType Ident ['[' ']' { '[' ConstExp ']' }]

//TODO: 记住i1类型要入哈希表!!!
//常量必须立即赋值, 变量可以只声明
//TODO: 这个C语法, 允许整型变量和数组变量同名吗. Ans: 不能!! yes
//TODO: 数组大小初始化不允许用变量表达式，即使该变量已被赋值。!!!!
//TODO:有返回值的函数Block的最后一句一定要显式地给出return语句，否则会当做“无返回语句”的错误处理。即使return语句是在if的各个分支里写的，且考虑到了所有分支执行的情况，但这样依然不合法。如果你认为你的分支已经覆盖了所有的返回情况，那么只需要在函数末尾再多加一条return就可以了